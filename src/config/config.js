var name = "web2"
var user = "postgres"
var password = "postgres"
const host = "localhost"
const port = "5432"

module.exports = {
    name: name,
    user: user,
    password: password,
    host: host,
    port: port
}